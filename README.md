# Chess Server

Chess Server is a standalone chess app with a `www/` Cordova-style layout.

## Notes

- The app still expects `cordova.js` and device-ready behavior, so it is being kept as a private repo for now.
- The `www/` directory remains the app root.
